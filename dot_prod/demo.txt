1. compile and run the serial version.
2. add in timers to see the relative work in each portion of the code.
3. show errors from NOT using private variables
4. show how to use the schedule clause:
     a. static
     c. runtime, use OMP_SCHEDULE to set (static n, dynamic, guided, etc.)

